Write a function that takes a pointer as a parameter and modifies the value it points to. Explain how this differs from call by reference.

Create a function that returns a reference to a variable. Then modify that reference in main() and explain why this works or where it can become dangerous.

Write a function that finds the maximum and minimum of three numbers using only return values, not global variables.

Design a function that performs input validation and uses early return to exit if the input is invalid. Explain why early return improves readability.

Write a function that swaps two numbers using call by value, then rewrite it using call by reference. Compare both results and reasoning.

Create a function declared before main() but defined after main(). Explain why the program still compiles.

Write a function that dynamically allocates memory inside it and returns the pointer. In main(), use the memory and then correctly free it. Explain what happens if you forget to free it.

Analyze a real-world task [for example, calculating monthly expenses or average rainfall over days] and break it into at least three well-named functions. Explain the responsibility of each function.